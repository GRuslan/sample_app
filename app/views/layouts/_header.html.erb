<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Система контролю доступу", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Головна", root_path %></li>
          <li><%= link_to "Допомога", help_path %></li>
          <% if signed_in? %>
            <li><%= link_to "Профіль", current_user %></li>
            <li> <%= link_to "Вихід", signout_path, method: "delete" %> </li>
            <% if admin_user? %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Accounts <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Налаштування", edit_user_path(current_user) %></li>
                <li><%= link_to "Працівники", users_path %></li>
                <li class="divider"></li>
                
              </ul>
            </li>
            <% end %>
          <% else %>
            <li><%= link_to "Вхід", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
